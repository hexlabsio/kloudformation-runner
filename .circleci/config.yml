version: 2
jobs:
  build:
    machine:
      enabled: true
    working_directory: ~/kloudformation-runner
    steps:
    - checkout

    - run:
        name: Transpile Stack
        command: ./kloudformation.sh Stack.kt Stack template.yml

    - store_artifacts:
        path: template.yml
