version: 2
jobs:
  build:
    machine:
      enabled: true
    working_directory: ~/kloudformation-runner
    steps:
    - checkout

    - run:
        name: Transpile Stack
        command: ./kloudformation.sh -q -stack-file stack/Stack.kt -stack-class CertInUsEast1

    - store_artifacts:
        path: template.yml
